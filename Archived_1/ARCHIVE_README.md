# Archived_1 – Pushable to GitHub

This folder contains the Bark project (code, configs, model metadata) with files ≤50MB.
Large files are in `../Archived_2/`. Large directories (images, plantnet_bark_images, FinnWoodlands*, zz.Archive) are excluded and remain in the original project.
