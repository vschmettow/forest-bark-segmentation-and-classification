# Utils

Utility scripts for data conversion, preprocessing, and helpers.

Shared functionality used by `code/scripts/`:
- Path helpers
- LabelMe / COCO / YOLO format conversion logic

For data conversion scripts, see `code/scripts/` (e.g. `convert_barknet_to_coco_and_yolo.py`, `create_staged_datasets.py`).
